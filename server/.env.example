# PocketBase 连接配置
PB_URL=http://localhost:8090
PB_ADMIN_EMAIL=admin@restaurant.com
PB_ADMIN_PASSWORD=REDACTED_DEFAULT_PASSWORD

# 服务监听配置
PORT=3000
HOST=127.0.0.1

# 环境
NODE_ENV=production

# 限流配置（每分钟最大请求数）
RATE_LIMIT_MAX=100

# CORS 配置（生产环境应限制为实际域名）
CORS_ORIGIN=*
